interface:
  display_name: "Git Commit"
  short_description: "Draft dotfiles commit messages"
  default_prompt: "Use $git-commit to draft a dotfiles commit message that follows this repository's style."
